name: chat_gpt_flutter
description: A Flutter ChatGPT web app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_bloc: any
  center_the_widgets: ^1.0.0+3
  url_strategy: any
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true